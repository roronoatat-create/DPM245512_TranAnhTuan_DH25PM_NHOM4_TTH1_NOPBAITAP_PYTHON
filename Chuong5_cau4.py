#Câu 4: Các hàm quan trọng trong xử lý chuỗi của Python
'''Đây là danh sách các hàm trong Python:

* `s.strip()`
* `s.lstrip()`
* `s.rstrip()`
* `s.split(sep)`
* `'ký_tự_nối'.join(list)`
* `s.find(sub)`
* `s.replace(old, new)`
* `s.upper()`
* `s.lower()`
* `s.capitalize()`
* `s.title()`
* `s.isupper()`
* `s.islower()`
* `s.isdigit()`
* `s.isalpha()`
* `s.isspace()`